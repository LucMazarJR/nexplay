import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Brand tokens
  static const brandSeed = Color(0xFF2979FF);
  static const brandPrimary = Color(0xFF85AAFF);

  // Neutral tokens
  static const neutralSurfaceDark = Color(0xFF121316);
}
